# Vista previa en Netlify (proyecto conectado desde la época de Webflow): compila Astro y publica dist/.
# El despliegue real es Cloudflare (wrangler.jsonc); aquí no corren el formulario ni el Worker.
[build]
  command = "npm run build"
  publish = "dist"
